#include <stdio.h>
#include <conio.h>

// int powers(int x, int mu, int kq);

// int Sum(int a, int b){
//     return a + b;
// }

// void Intong(int a, int b, int c);

int main(){

    // Example 1:

    // int a = 0;
    // int b = 0;
    // int n = 0;
    // int kq = 1;

    // printf("Enter number A: ");
    // scanf("%d", &a);

    // printf("Enter number B: ");
    // scanf("%d", &b);

    // printf("Enter number N: ");
    // scanf("%d", &n);

    // printf("\n %d mu %d=%d ", a, n, powers(a, n, kq));
    // printf("\n %d mu %d=%d ", b, n, powers(b, n, kq));

    // printf("%d", a, n, kq);

    // Example 2:

    // int x = 5;
    // int y = 7;
    // int z = 3;
    // int k = 4;

    // printf("Sum of %d, %d, %d, %d, is: %d\n", x, y, z, k, Sum(Sum(x, y), Sum(z,k)));

    // Example 3:

    // int a = 10;
    // int b = 20;
    // int c = 30;

    // Example 4:

    

    return 0;
}

// void Intong(int a, int b, int c){
//     printf("\n Sum of a+b+c= %d", a+b+c);
// }

// int powers(int x, int mu, int kq){
//     for(int j = 1; j <= mu; j++){
//         kq = kq * x;
//     }

//     return kq;

// }